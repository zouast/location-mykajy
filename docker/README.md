# Docker configuration notes

## PostgreSQL + pgAdmin
Run the following from the project root to start all services:
```bash
docker compose up -d
```

Services:
- PostgreSQL: localhost:5432
- pgAdmin: http://localhost:5050 (admin@mykajy.local / admin)
